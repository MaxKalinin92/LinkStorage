import React from 'react';
import './addLink.css';

const addLink = (props) => {
    return (
      <button 
        className="AddFolder-button" 
        type="submit">Add Link
      </button>
    )
}

export default addLink